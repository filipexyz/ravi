# Runbook

1. Inspect the provider registry and capability matrix.
2. Configure explicit targets and policy at an approved scope.
3. Use the explain command to verify eligibility and provenance.
4. Observe considered, rejected, selected, switched and exhausted trace events.
5. Disable the policy to return immediately to single-target behavior.

Never add implicit fallback based only on installed credentials.
