# CRM Domain / WHY

The CRM domain is the umbrella for contacts, accounts, opportunities, pipelines,
and relationship operations. Its current `SPEC.md` is intentionally minimal and
must not be treated as a complete product contract on its own.

Use concrete child specs such as `crm/pipeline` and contact authorization specs
for enforceable behavior until this domain spec is expanded with real
invariants.
