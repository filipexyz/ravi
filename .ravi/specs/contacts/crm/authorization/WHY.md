# CRM Authorization / WHY

CRM data can reveal more than the contact record itself: account membership,
opportunities, tasks, activities, facts, notes, health, lifecycle, and next
actions. A CRM read path must not become a privacy bypass around hidden
contacts.

By default, contact-linked CRM data inherits contact visibility. Future CRM
object grants may narrow this, but broad runtime CRM reads are not open.
