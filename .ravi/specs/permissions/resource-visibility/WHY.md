# Resource Visibility / WHY

Discovery is disclosure. Listing, showing, checking, autocomplete, alias
resolution, SDK discovery, and UI pickers can reveal installed apps, sessions,
agents, contacts, chats, operational state, and relationships.

This capability forces discovery surfaces to apply the same effective runtime
context as execution, while preserving direct local CLI inspection for explicit
operator paths.
