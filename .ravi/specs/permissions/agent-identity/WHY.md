# Agent Identity Authority / WHY

The active multiplayer model asks what the agent identity can do in the current
compartment. It does not default to the personal capabilities of the human who
typed in a shared chat.

This keeps a team agent usable in shared surfaces while still failing closed for
unresolved external actors and preserving actor/contact as provenance instead of
ambient tool authority.
