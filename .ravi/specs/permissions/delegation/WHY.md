# Delegation / WHY

Delegation defines how authority is lent to an agent for one execution. It is
explicit, auditable, and bounded: broad agent grants are a technical ceiling,
not ambient authority for anyone who can speak to that agent.

The model prevents raw chat access from becoming tool access, keeps unknown
actors fail-closed, and separates ordinary delegated authority from break-glass
operator recovery.
