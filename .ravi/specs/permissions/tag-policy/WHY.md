# Why

Tags are useful for grouping contacts and resources, but ambient tag authority
is too easy to misread. Permission-scoped tags must be consumed by an explicit
provider so the authorization path remains explainable and testable.
