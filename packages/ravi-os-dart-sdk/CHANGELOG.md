# Changelog

## 0.1.0

- Initial generated Dart client over the Ravi `RegistrySnapshot`.
- Published pub name is `ravi_sdk`.
- HTTP-only transport with Bearer context-key auth.
- Official SSE stream client for `/api/v1/_stream/<channel>`.
